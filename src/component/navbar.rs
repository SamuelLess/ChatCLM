use leptos::{component, view, IntoView};
// use crate::component::dropdown::Dropdown;

#[component]
pub fn NavBar() -> impl IntoView {
    view! {
        <nav class="navbar">
            // <Dropdown />
        </nav>
    }
}
